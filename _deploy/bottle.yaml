bottle:
  name: AquaCore

services:
  aqua-core:
    image: aqua-core:latest
    tty: true
    cap-add: [CAP_NET_ADMIN]
    device:
      - /dev/net/tun
    network: raind0
    ports:
      - "18888:8080"
